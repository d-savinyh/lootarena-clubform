---
trigger: always_on
---

Ничего разрушительного (DROP/TRUNCATE/массовые DELETE, выключение workflow, перезапись секретов) не делать без явного подтверждения от меня.

Если задача затрагивает данные — предложи безопасный план (бэкап/транзакция/миграция/rollback).
